<Window x:Class="UchetOnline.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Вход"
        Height="320" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Background="{StaticResource SecondaryBrush}">
        <Border Background="White" CornerRadius="12" Padding="32" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Добро пожаловать" FontSize="24" FontWeight="SemiBold" />
                <TextBlock Text="Введите учетные данные" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,24" />
                <TextBlock Text="Логин" />
                <TextBox Margin="0,4,0,16" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Пароль" />
                <PasswordBox x:Name="PasswordBox" Margin="0,4,0,16" PasswordChanged="PasswordBox_OnPasswordChanged" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,12" />
                <Button Content="Войти" Command="{Binding LoginCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
