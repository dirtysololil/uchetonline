<Window x:Class="UchetOnline.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Учет Онлайн"
        Height="720" Width="1180"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Background="White" Margin="16" CornerRadius="12">
            <StackPanel>
                <TextBlock Text="Учет Онлайн" FontSize="24" FontWeight="SemiBold" Margin="16" />
                <ListBox x:Name="ModuleList"
                         ItemsSource="{Binding Modules}"
                         SelectedItem="{Binding SelectedModule, Mode=TwoWay}"
                         SelectionChanged="ModuleList_OnSelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
        <ScrollViewer Grid.Column="1" Margin="0,16,16,16" VerticalScrollBarVisibility="Auto">
            <ContentControl Content="{Binding CurrentModule}" />
        </ScrollViewer>
    </Grid>
</Window>
